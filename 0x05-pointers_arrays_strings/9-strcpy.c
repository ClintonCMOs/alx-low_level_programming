#include "main.h"
#include "2-strlen.c"

/**
 * _strcpy - copies string pointed
 * @dest: destination to copy strings
 * @src: src
 *
 * Return: A character
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
