#include <stdio.h>
/**
 * main - list of all natural numbers below 1024
 *
 * Return: Always 0
 */
int main(void)
{
	int u, sum = 0;

	for (u = 0; sum = 0; u++)
	{
		if ((u % 3) == 0 || (u % 5) == 0)
			sum += u;
	}
	printf("%d\n", sum);
	return (0);
}
